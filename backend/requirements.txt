# Backend Python bağımlılıkları.
# Kurulum: python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt

# MariaDB/MySQL veritabanı sürücüsü (Repository katmanı kullanır)
PyMySQL==1.2.0

# PostgreSQL sürücüsü (host replikası; common/db_pg.py + yetki_repository kullanır).
# Uygulamanın MariaDB bağlantısından bağımsızdır, onun yerini ALMAZ.
# [binary]: derleyici/libpq gerektirmeyen hazır ikili paket.
psycopg[binary]==3.2.9

# Şifre hash'leme (yavaş/adaptive; auth modülü login doğrulamada kullanır)
bcrypt==5.0.0

# HTML sanitizasyon (XSS) — soru_metni biçimli HTML tutar; Service gösterim
# öncesi sanitize eder (migration 007). allowlist tabanlı, script/style/olay
# yakalayıcı ve javascript: şeması temizlenir.
nh3==0.3.6

# HTTP/sınır katmanı web framework'ü (Controller'ı HTTP'ye açan api/ adaptörü)
# Pydantic v2 fastapi ile transitively gelir.
fastapi==0.115.6

# ASGI sunucusu (uvicorn[standard]: uvloop/httptools ile hızlı çalıştırma)
uvicorn[standard]==0.34.0

# Excel (.xlsx) okuma/yazma — toplu soru yükleme şablonunun üretimi ve yüklenen
# dosyanın satırlara çözümlenmesi (sınır/Controller katmanı; iş kuralı içermez).
openpyxl==3.1.5

# multipart/form-data ayrıştırıcı; FastAPI'nin UploadFile (dosya yükleme) ucu için
# gereklidir (toplu yükleme POST /soru/post/toplu-yukle).
python-multipart==0.0.20
